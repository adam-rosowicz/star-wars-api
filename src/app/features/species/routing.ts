import express from "express";

// VALIDATION_IMPORTS

export interface SpeciesRoutingDependencies {
  // ACTIONS_IMPORTS
}

export const speciesRouting = () => {
  const router = express.Router();

  // ACTIONS_SETUP

  return router;
};
