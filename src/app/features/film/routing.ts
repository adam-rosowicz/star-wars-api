import express from "express";

// VALIDATION_IMPORTS

export interface FilmRoutingDependencies {
  // ACTIONS_IMPORTS
}

export const filmRouting = () => {
  const router = express.Router();

  // ACTIONS_SETUP

  return router;
};
