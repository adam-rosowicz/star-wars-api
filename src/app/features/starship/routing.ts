import express from "express";

// VALIDATION_IMPORTS

export interface StarshipRoutingDependencies {
  // ACTIONS_IMPORTS
}

export const starshipRouting = () => {
  const router = express.Router();

  // ACTIONS_SETUP

  return router;
};
