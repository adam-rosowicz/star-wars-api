import express from "express";

// VALIDATION_IMPORTS

export interface PlanetRoutingDependencies {
  // ACTIONS_IMPORTS
}

export const planetRouting = () => {
  const router = express.Router();

  // ACTIONS_SETUP

  return router;
};
