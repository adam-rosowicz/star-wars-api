import express from "express";

// VALIDATION_IMPORTS

export interface VehicleRoutingDependencies {
  // ACTIONS_IMPORTS
}

export const vehicleRouting = () => {
  const router = express.Router();

  // ACTIONS_SETUP

  return router;
};
